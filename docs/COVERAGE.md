# Widescreen evidence coverage

Generated by tools/coverage_explorer.py — do not edit.

| status | entrances |
|---|---|
| degraded | 1 |
| centered-only | 3 |
| never-observed | 252 |

## Next-evidence worklist (cheapest first)

- `$16` **JungleHijinxs_Main** — degraded via capture_jungle_snapshots.dks, route_death.dks: fix fallbacks/blanks, then re-sweep
- `$EC` **KongoJungleMap** — centered-only via capture_jungle_snapshots.dks, route_death.dks: record a widescreen leg of the existing route
- `$01` **ReptileRumble_Main2** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$07` **BouncyBonanza_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$0A` **MistyMine_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$0C` **RopeyRampage_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$0D` **OrangutanGang_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$12` **ManicMincers_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$14` **TorchlightTrouble_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$17` **BarrelCannonCanyon_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$18` **ElevatorAntics_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$21` **UnusedRoom_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$22` **PoisonPond_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$24` **SnowBarrelBlast_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$27` **MinecartMadness_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$2B` **PlatformPerils_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$2E` **MineCartCarnage_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$2F` **TrickTrackTrek_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$30` **TankedUpTrouble_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$31` **StopAndGoStation_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$34` **AnimalTokenTest_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$36` **LoopyLights_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$3E` **CroctopusChase_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$40` **OilDrumAlley_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)
- `$41` **BlackoutBasement_Main** — never-observed: record a route (fresh-entry sweep can seed a snapshot)

Full table: build/coverage.json (256 entrances).
